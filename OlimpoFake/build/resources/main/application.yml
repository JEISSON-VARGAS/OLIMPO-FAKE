server:
  port: 8082

spring:
  application:
    name: olimpo_back
  datasource:
    url:  jdbc:postgresql://localhost:5432/OlimpoDev
    username: postgres
    password: 'fabrica2024*'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      #ddl-auto: create-drop
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect